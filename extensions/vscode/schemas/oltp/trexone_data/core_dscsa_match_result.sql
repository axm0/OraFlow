-- ==========================================

  CREATE TABLE "TREXONE_DATA"."CORE_DSCSA_MATCH_RESULT"
   (	"CF_FACILITY_KEY" NUMBER(18,0) NOT NULL ENABLE,
	"MATCH_DATE" DATE NOT NULL ENABLE,
	"NDC" VARCHAR2(15) NOT NULL ENABLE,
	"PRODUCT_NAME" VARCHAR2(40),
	"LOT_NUMBER" VARCHAR2(20),
	"SERIAL_NUMBER" VARCHAR2(20),
	"EXPIRATION_DATE" VARCHAR2(8),
	"SUPPLIER_NAME" VARCHAR2(30),
	"PO_NUMBER" VARCHAR2(50),
	"SUPPLIER_PO_NUMBER" VARCHAR2(50),
	"COMPARISON_TYPE" VARCHAR2(20) NOT NULL ENABLE,
	"MATCH_USER_ID" VARCHAR2(255) NOT NULL ENABLE,
	"EXCEPTION_STATUS_CODE" CHAR(1) NOT NULL ENABLE,
	"MATCH_RESULT_CODE" CHAR(1) NOT NULL ENABLE,
	"ERX_CLIENT_ID" VARCHAR2(100) DEFAULT SYS_CONTEXT('ERX_CONTEXT', 'ERX_CLIENT_ID') NOT NULL ENABLE,
	"PARTITION_DATE" DATE DEFAULT SYSDATE NOT NULL ENABLE,
	 CONSTRAINT "FK_CORE_FACILITY_15" FOREIGN KEY ("CF_FACILITY_KEY")
	  REFERENCES "TREXONE_DATA"."CORE_FACILITY" ("CF_FACILITY_KEY") ENABLE NOVALIDATE
   )
  PARTITION BY RANGE ("PARTITION_DATE") INTERVAL (NUMTOYMINTERVAL(3,'MONTH')) STORE IN ("MEDIUM_DATA01")
 (PARTITION "P_MINVALUE"  VALUES LESS THAN (TO_DATE(' 2010-01-01 00:00:00', 'SYYYY-MM-DD HH24:MI:SS', 'NLS_CALENDAR=GREGORIAN')) ) ;
-- ==========================================

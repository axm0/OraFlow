-- ==========================================

  CREATE GLOBAL TEMPORARY TABLE "TREXONE_AUD_DATA"."GTT_PATS"
   (	"PATIENT_NUM" NUMBER(18,0)
   ) ON COMMIT PRESERVE ROWS ;
-- ==========================================

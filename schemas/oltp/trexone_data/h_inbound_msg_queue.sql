-- ==========================================

  CREATE TABLE "TREXONE_DATA"."H_INBOUND_MSG_QUEUE"
   (	"INBOUND_MSG_QUEUE_NUM" NUMBER(18,0),
	"OUTBOUND_MSG_QUEUE_NUM" NUMBER(18,0),
	"HTTP_HEADERS" VARCHAR2(4000),
	"HTTP_METHOD" VARCHAR2(20),
	"REQUEST_CONTENT" CLOB NOT NULL ENABLE,
	"HTTP_STATUS" NUMBER(18,0) NOT NULL ENABLE,
	"RESPONSE_CONTENT" CLOB,
	"PARTITION_DATE" DATE NOT NULL ENABLE,
	"REQUEST_URI" VARCHAR2(4000) NOT NULL ENABLE,
	"SYS_USER" NUMBER(18,0),
	"DATESTAMP" DATE,
	"NUM_UPDATES" NUMBER(18,0),
	"ERX_CLIENT_ID" VARCHAR2(100),
	"H_TYPE" CHAR(1),
	"H_LEVEL" NUMBER
   )
  PARTITION BY RANGE ("DATESTAMP") INTERVAL (NUMTODSINTERVAL(1, 'DAY'))
 (PARTITION "P_MINVALUE"  VALUES LESS THAN (TO_DATE(' 2013-07-01 00:00:00', 'SYYYY-MM-DD HH24:MI:SS', 'NLS_CALENDAR=GREGORIAN')) ) ;
-- ==========================================

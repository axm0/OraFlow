-- ==========================================

  CREATE TABLE "TREXONE_DATA"."WIPE_EXCLUDE_LIST"
   (	"SCHEMA_NAME" VARCHAR2(30) NOT NULL ENABLE,
	"TABLE_NAME" VARCHAR2(30) NOT NULL ENABLE,
	"COLUMN_NAME" VARCHAR2(30)
   ) ;
-- ==========================================

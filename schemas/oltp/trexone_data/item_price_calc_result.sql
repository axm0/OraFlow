-- ==========================================

  CREATE TABLE "TREXONE_DATA"."ITEM_PRICE_CALC_RESULT"
   (	"ITEM_PRICE_CALC_RESULT_NUM" NUMBER(18,0) NOT NULL ENABLE,
	"ORDER_NUM" NUMBER(18,0) NOT NULL ENABLE,
	"ITEM_SEQ" NUMBER(18,0) NOT NULL ENABLE,
	"PRICE_CALC_RESULT_NUM" NUMBER(18,0),
	"IS_CURRENT" CHAR(1) NOT NULL ENABLE,
	"ARCHIVE_LEVEL" NUMBER(9,0) NOT NULL ENABLE,
	"SYS_USER" NUMBER(18,0) NOT NULL ENABLE,
	"DATESTAMP" DATE NOT NULL ENABLE,
	"NUM_UPDATES" NUMBER(9,0) NOT NULL ENABLE,
	"PARTITION_KEY" NUMBER(18,0) NOT NULL ENABLE,
	"PARTITION_DATE" DATE NOT NULL ENABLE,
	"ERX_CLIENT_ID" VARCHAR2(100) DEFAULT sys_context('ERX_CONTEXT', 'ERX_CLIENT_ID') NOT NULL ENABLE,
	 CONSTRAINT "FK_ITEM_08" FOREIGN KEY ("ORDER_NUM", "ITEM_SEQ")
	  REFERENCES "TREXONE_DATA"."ITEM" ("ORDER_NUM", "ITEM_SEQ") ENABLE,
	 CONSTRAINT "FK_PRICE_CALC_RESULT_01" FOREIGN KEY ("PRICE_CALC_RESULT_NUM")
	  REFERENCES "TREXONE_DATA"."PRICE_CALC_RESULT" ("PRICE_CALC_RESULT_NUM") ENABLE NOVALIDATE
   )
  PARTITION BY RANGE ("PARTITION_DATE")
 (PARTITION "P_MAXVALUE"  VALUES LESS THAN (MAXVALUE) )  ENABLE ROW MOVEMENT ;
  CREATE UNIQUE INDEX "TREXONE_DATA"."PK_ITEM_PRICE_CALC_RESULT" ON "TREXONE_DATA"."ITEM_PRICE_CALC_RESULT" ("ITEM_PRICE_CALC_RESULT_NUM")
  ;
ALTER TABLE "TREXONE_DATA"."ITEM_PRICE_CALC_RESULT" ADD CONSTRAINT "PK_ITEM_PRICE_CALC_RESULT" PRIMARY KEY ("ITEM_PRICE_CALC_RESULT_NUM")
  USING INDEX "TREXONE_DATA"."PK_ITEM_PRICE_CALC_RESULT"  ENABLE;
-- ==========================================
